# Harness additions for /boot/loader.conf.
# DragonFlyBSD's loader does not accept FreeBSD's comma-separated console
# list. Use single value "comconsole" for serial-only (we don't need VGA
# once installed).
console="comconsole"
comconsole_speed="115200"
boot_serial="YES"
